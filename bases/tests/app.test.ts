// A A A
// arrange, act, assert

describe("Test in the App File", () => {

  it("should be 30", () => {

    // Arrange
    const number1 = 10;
    const number2 = 20;

    // Act
    const result = number1 + number2;

    // Assert
    expect(result).toBe(30);

  });

});
